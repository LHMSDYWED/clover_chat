server:
  port: 12000
spring:
  application:
    name: config
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/clover_buolal/wehcat_dev.git
  rabbitmq:
    host: 120.79.93.99
management: #暴露触发消息总线的地址
  endpoints:
    web:
      exposure:
        include: bus-refresh